## 格式语法测试

- 这是一个测试文件，这是测试文本

---



```html
<!doctype html>
<html lang="zh-CN">
  <head><meta charset="utf-8"><title>Demo</title></head>
  <body><h1>Hello, Ornata</h1></body>
</html>
```

```css
:root { --accent: #4f46e5; }
body { font-family: system-ui; color: #111827; }
```

```javascript
const greet = (name) => `Hello ${name}`;
console.log(greet("Ornata"));
```

```typescript
type User = { id: number; name: string };
const user: User = { id: 1, name: "Ornata" };
```

```tsx
export function Button({ label }: { label: string }) {
  return <button className="btn">{label}</button>;
}
```
```json
{
  "name": "ornata",
  "version": "0.1.0",
  "private": true
}
```

```yaml
app:
  name: Ornata
  theme: dark
```

```bash
set -e
echo "build ok"
```

```python
def fib(n: int) -> int:
    return n if n < 2 else fib(n-1) + fib(n-2)
```

```go
package main
import "fmt"
func main() { fmt.Println("Hello") }
```

```java
class Main {
  public static void main(String[] args) {
    System.out.println("Hello");
  }
}
```

```rust
fn main() {
    println!("Hello, Ornata");
}
```
```sql
SELECT id, name FROM users WHERE active = 1 ORDER BY id DESC;
```
```cpp
#include <iostream>
int main() { std::cout << "Hi\n"; }
```

```csharp
var items = new List<int> {1, 2, 3};
Console.WriteLine(items.Count);
```
```php
<?php
echo "Hello Ornata";
```
